print("This program calculates the area of a triangle using A = 0.5*b*h")
base = float(input("  Base length in centimetres: "))
height = float(input("  Height in centimetres: "))

area = 0.5 * base * height

print(f"Area = {area} cm^2")